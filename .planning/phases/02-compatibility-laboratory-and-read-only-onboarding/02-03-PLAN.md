---
phase: 02-compatibility-laboratory-and-read-only-onboarding
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - plugin/src/main/kotlin/org/jmixworkbench/discovery/safety/DiscoveryEffects.kt
  - plugin/src/test/kotlin/org/jmixworkbench/discovery/safety/MutationTripwire.kt
  - plugin/src/test/kotlin/org/jmixworkbench/discovery/safety/MutationTripwireTest.kt
autonomous: true
requirements: [DISC-01, TEAM-07]
must_haves:
  truths:
    - "Every production collector receives one explicit read/effect boundary; process, socket, JDBC, file-store, IDE write, and VFS mutation channels are deny-only."
    - "Intentional trips for every prohibited channel fail, while clean read-only capture remains byte-identical."
    - "Fixture hashing never follows symlinks or mutates real IDE/VCS state."
  artifacts:
    - path: "plugin/src/main/kotlin/org/jmixworkbench/discovery/safety/DiscoveryEffects.kt"
      provides: "Enforceable production I/O/effect interfaces"
      contains: "interface DiscoveryEffects"
    - path: "plugin/src/test/kotlin/org/jmixworkbench/discovery/safety/MutationTripwire.kt"
      provides: "Zero-mutation observation harness"
      contains: "class MutationTripwire"
  key_links:
    - from: "plugin/src/test/kotlin/org/jmixworkbench/discovery/safety/MutationTripwireTest.kt"
      to: "plugin/src/main/kotlin/org/jmixworkbench/discovery/safety/DiscoveryEffects.kt"
      via: "intentional trip tests for every channel"
      pattern: "assertForbidden"
---

<objective>
Create the enforceable production effect boundary and intentional zero-mutation tripwire before collectors are implemented.

Purpose: Ledger-only observation is insufficient unless direct bypass is structurally forbidden and tested.
Output: Production effect interfaces, deny-only mutation channels, inline synthetic fixture builders, and intentional trip tests.
</objective>
<execution_context>
@/Users/a.k.mmuhibullahnayem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a.k.mmuhibullahnayem/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/02-compatibility-laboratory-and-read-only-onboarding/02-VALIDATION.md
@.planning/phases/02-compatibility-laboratory-and-read-only-onboarding/02-RESEARCH.md
@plugin/src/main/kotlin/org/jmixworkbench/discovery/model/DiscoveryModel.kt
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define production read and deny-only effect interfaces</name>
  <files>plugin/src/main/kotlin/org/jmixworkbench/discovery/safety/DiscoveryEffects.kt, plugin/src/test/kotlin/org/jmixworkbench/discovery/safety/MutationTripwireTest.kt</files>
  <read_first>plugin/src/main/kotlin/org/jmixworkbench/discovery/safety/DiscoveryEffects.kt (CREATE), .planning/phases/02-compatibility-laboratory-and-read-only-onboarding/02-RESEARCH.md, plugin/src/main/kotlin/org/jmixworkbench/services/JmixProjectService.kt</read_first>
  <behavior>
    - Collectors can request bounded contained reads and IDE read snapshots only.
    - Process launch, network/socket, JDBC/data-source, file-store, write-command/write-action, VFS save/refresh/write, and external-system sync APIs return `P2_EFFECT_FORBIDDEN`.
    - No production implementation can silently substitute a permissive mutation channel.
  </behavior>
  <action>Define `DiscoveryEffects` with `TargetFileReader` and `ImportedModelReader` read capabilities plus explicit `ProcessEffect`, `NetworkEffect`, `DatabaseEffect`, `FileStoreEffect`, `IdeWriteEffect`, `VfsMutationEffect`, and `BuildExecutionEffect` interfaces. Supply only deny-only production implementations for prohibited channels, each emitting `P2_EFFECT_FORBIDDEN`; require all collector constructors created in later plans to accept this aggregate instead of calling JVM/IDE APIs directly.</action>
  <verify><automated>cd plugin &amp;&amp; ./gradlew :idea253:test --tests 'org.jmixworkbench.discovery.safety.MutationTripwireTest.effectBoundary*' phase2FastCheck --dependency-verification=strict --no-daemon</automated></verify>
  <acceptance_criteria>`rg -n 'interface DiscoveryEffects|ProcessEffect|NetworkEffect|DatabaseEffect|IdeWriteEffect|P2_EFFECT_FORBIDDEN' plugin/src/main/kotlin/org/jmixworkbench/discovery/safety/DiscoveryEffects.kt` finds every contract; focused tests intentionally call each deny-only channel and exit 0.</acceptance_criteria>
  <done>Production discovery has one injectable effect boundary and no permissive mutation implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement byte/metadata tripwire and intentional channel trips</name>
  <files>plugin/src/test/kotlin/org/jmixworkbench/discovery/safety/MutationTripwire.kt, plugin/src/test/kotlin/org/jmixworkbench/discovery/safety/MutationTripwireTest.kt</files>
  <read_first>plugin/src/test/kotlin/org/jmixworkbench/discovery/safety/MutationTripwire.kt (CREATE), plugin/src/main/kotlin/org/jmixworkbench/discovery/safety/DiscoveryEffects.kt, .planning/phases/02-compatibility-laboratory-and-read-only-onboarding/02-VALIDATION.md</read_first>
  <behavior>
    - Capture compares source/config/build, `.idea`, `.gradle`, and `.git` bytes/metadata without following links.
    - Intentional file/process/socket/DNS/HTTP/JDBC/file-store/write-action/write-command/VFS/sync trips each make the test fail.
    - Clean capture/compare has empty ledgers and byte-identical state.
  </behavior>
  <action>Implement `MutationTripwire.capture()`/`assertUnchanged()` with canonical contained no-follow hashing and event ledgers for all prohibited effect interfaces. Add one intentional trip test per channel plus clean/cancel/dispose cases. Build every early tripwire repository with inline `SyntheticTripwireFixture` builders under the test temporary/build directory; do not create or read tracked fixture JSON in this plan. Builders expose fixture ID/provenance/expected markers in Kotlin, forbid credentials, proprietary coordinates, and absolute paths, and are deleted after comparison.</action>
  <verify><automated>cd plugin &amp;&amp; ./gradlew :idea253:test --tests 'org.jmixworkbench.discovery.safety.MutationTripwireTest' phase2FastCheck --dependency-verification=strict --no-daemon</automated></verify>
  <acceptance_criteria>`rg -n 'process|socket|jdbc|fileStore|writeAction|writeCommand|vfs|sync|SyntheticTripwireFixture' plugin/src/test/kotlin/org/jmixworkbench/discovery/safety/MutationTripwireTest.kt` finds every intentional trip and inline builder; `test ! -e plugin/fixtures/phase2/manifest.json` succeeds at this wave; tests exit 0.</acceptance_criteria>
  <done>The harness proves it detects every prohibited effect, not merely file-diff mutations.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Collector → JVM/IDE/OS | A bypass could execute or mutate outside the fixture ledger. |
| Fixture path → tripwire | Symlinks/traversal could read or hash outside the test root. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T2-BYPASS-03 | Elevation of privilege | effect boundary | mitigate | Constructor injection, deny-only channels, and intentional trip tests. |
| T2-MUTATE-03 | Tampering | tripwire | mitigate | Byte/metadata snapshots plus per-channel ledgers. |
| T2-PATH-03 | Information disclosure | hashing | mitigate | Canonical containment and no-follow traversal. |
</threat_model>
<verification>Run the intentional-trip suite and inspect the inline builders for sensitive fields.</verification>
<success_criteria>
- Every prohibited channel has an enforceable interface and a failing intentional-trip case.
- Clean capture is byte-identical with empty ledgers.
- Inline tripwire fixtures contain no secrets, proprietary metadata, or path escape.
</success_criteria>
<output>After completion, create `.planning/phases/02-compatibility-laboratory-and-read-only-onboarding/02-03-SUMMARY.md`</output>
