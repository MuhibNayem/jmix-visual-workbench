---
phase: 02-compatibility-laboratory-and-read-only-onboarding
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/build.gradle.kts
  - plugin/src/main/kotlin/org/jmixworkbench/discovery/model/DiscoveryModel.kt
  - plugin/src/main/kotlin/org/jmixworkbench/discovery/model/CanonicalDiscoveryJson.kt
  - plugin/src/phase2CoreTest/kotlin/org/jmixworkbench/discovery/PlatformIndependenceTest.kt
  - plugin/src/phase2CoreTest/kotlin/org/jmixworkbench/discovery/model/DiscoveryModelTest.kt
  - plugin/src/phase2CoreTest/kotlin/org/jmixworkbench/discovery/model/CanonicalDiscoveryJsonTest.kt
autonomous: true
requirements: [COMP-01, DISC-07, TEAM-07]
must_haves:
  truths:
    - "Pure discovery contracts and serialization run without any IntelliJ Platform dependency."
    - "Identical normalized facts always produce byte-identical JSON and SHA-256 identities."
    - "Source locations contain a safe relative locator and fingerprint, never an opaque runtime handle or machine path."
    - "Optional IDE capabilities remain host-neutral evidence with explicit presence, enablement, version, source, confidence, and diagnostics."
  artifacts:
    - path: "plugin/src/main/kotlin/org/jmixworkbench/discovery/model/DiscoveryModel.kt"
      provides: "Immutable discovery/evidence/profile/artifact/relationship contracts"
      contains: "data class DiscoverySnapshot"
    - path: "plugin/src/main/kotlin/org/jmixworkbench/discovery/model/CanonicalDiscoveryJson.kt"
      provides: "Platform-independent canonical serialization and fingerprinting"
      contains: "object CanonicalDiscoveryJson"
  key_links:
    - from: "plugin/build.gradle.kts"
      to: "plugin/src/phase2CoreTest/kotlin"
      via: "phase2Core source set and phase2CoreTest task"
      pattern: "phase2CoreTest"
---

<objective>
Create the platform-independent Wave 0 discovery model, safe source-locator contract, deterministic serialization, and pure test source set.

Purpose: Host tests must consume already-proven contracts instead of being the first place core behavior can run.
Output: Pure Kotlin model/serializer files and a sub-60-second `phase2CoreTest` task with no IntelliJ classpath.
</objective>

<execution_context>
@/Users/a.k.mmuhibullahnayem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a.k.mmuhibullahnayem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-compatibility-laboratory-and-read-only-onboarding/02-RESEARCH.md
@.planning/phases/02-compatibility-laboratory-and-read-only-onboarding/02-VALIDATION.md
@plugin/build.gradle.kts
@plugin/src/main/kotlin/org/jmixworkbench/model/ProjectConfig.kt
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Establish the IntelliJ-free core test lane</name>
  <files>plugin/build.gradle.kts, plugin/src/phase2CoreTest/kotlin/org/jmixworkbench/discovery/PlatformIndependenceTest.kt</files>
  <read_first>plugin/build.gradle.kts, plugin/gradle/libs.versions.toml, plugin/hosts/idea253/build.gradle.kts, plugin/hosts/idea262/build.gradle.kts, .planning/phases/02-compatibility-laboratory-and-read-only-onboarding/02-VALIDATION.md</read_first>
  <behavior>
    - `phase2CoreTest` compiles only pure discovery Kotlin and never resolves IntelliJ artifacts.
    - The task covers model, fingerprint, canonical serialization, registry, and parser test packages as those files are added.
    - Warm execution completes within 60 seconds and produces JUnit XML.
  </behavior>
  <action>Apply the pinned Kotlin JVM plugin to the aggregate build and define a `phase2Core` source set containing only `org/jmixworkbench/discovery/model/**`, `compatibility/**`, the pure `static/GradleConfigParser.kt`, and the single resource `compatibility/phase2-registry.json`; define `phase2CoreTest` from `src/phase2CoreTest/kotlin` with Kotlin test/JUnit dependencies and no IntelliJ Platform dependency. Register non-watch `phase2CoreTest` and the initial growing `phase2FastCheck`, which depends only on `phase2CoreTest` at this wave; Plans 03-04 extend the same aggregate with tripwire/scanner/exact-host lanes as those artifacts become available. Add `PlatformIndependenceTest` to inspect its runtime classpath and fail on `com.intellij`, `org.jetbrains.intellij`, or IDE distribution artifacts.</action>
  <verify><automated>cd plugin &amp;&amp; ./gradlew phase2CoreTest --tests 'org.jmixworkbench.discovery.PlatformIndependenceTest' phase2FastCheck --dependency-verification=strict --no-daemon</automated></verify>
  <acceptance_criteria>`./gradlew tasks --all | rg 'phase2CoreTest|phase2FastCheck'` finds both tasks; `./gradlew dependencies --configuration phase2CoreTestRuntimeClasspath | ! rg 'com\\.jetbrains\\.intellij'` exits 0; the focused platform-independence test and initial fast check exit 0.</acceptance_criteria>
  <done>A fast platform-independent test lane exists before either exact host lane is used.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define immutable contracts and canonical identities</name>
  <files>plugin/src/main/kotlin/org/jmixworkbench/discovery/model/DiscoveryModel.kt, plugin/src/main/kotlin/org/jmixworkbench/discovery/model/CanonicalDiscoveryJson.kt, plugin/src/phase2CoreTest/kotlin/org/jmixworkbench/discovery/model/DiscoveryModelTest.kt, plugin/src/phase2CoreTest/kotlin/org/jmixworkbench/discovery/model/CanonicalDiscoveryJsonTest.kt</files>
  <read_first>plugin/src/main/kotlin/org/jmixworkbench/discovery/model/DiscoveryModel.kt (CREATE), plugin/src/main/kotlin/org/jmixworkbench/model/ProjectConfig.kt, .planning/phases/02-compatibility-laboratory-and-read-only-onboarding/02-RESEARCH.md</read_first>
  <behavior>
    - Input ordering and host-only incidental values do not change canonical JSON or SHA-256 IDs.
    - Unknown/conflicting evidence remains explicit and never becomes `2.4.0`, PostgreSQL, `com.example.app`, or `src/main/*`.
    - `SourceLocator` stores project-relative path, symbol, line/column, and revision fingerprint only; no `navigationId`, Project, PSI, VFS, or absolute path.
    - `OptionalIdeCapability(id, present, enabled, version, source, confidence, diagnostic)` preserves exact present/enabled/version evidence; missing or conflicting observations never become installed/enabled.
  </behavior>
  <action>Create JSON-serializable `Evidence&lt;T&gt;`, `DiscoverySnapshot`, `BuildSnapshot`, `ModuleSnapshot`, `SourceRootSnapshot`, `DependencyFact`, `OptionalIdeCapability`, `JmixProfile`, `ArtifactSnapshot`, `ArtifactRelationship`, `DiscoveryDiagnostic`, `SourceLocator`, and `CompatibilityDecision`. `OptionalIdeCapability` has exactly `id`, `present`, `enabled`, `version`, `source`, `confidence`, and `diagnostic`, is included in the snapshot/profile, and uses explicit unknown/conflicting evidence rather than defaults. Define exact confidence/state/role/relationship enums from research. Implement sorted UTF-8 canonical JSON and SHA-256 helpers in `CanonicalDiscoveryJson`; IDs use kind/build/module/normalized semantic key, and serialization excludes timestamps, absolute paths, runtime handles, and IDE objects.</action>
  <verify><automated>cd plugin &amp;&amp; ./gradlew phase2CoreTest --tests 'org.jmixworkbench.discovery.model.*' phase2FastCheck --dependency-verification=strict --no-daemon</automated></verify>
  <acceptance_criteria>`rg -n 'data class DiscoverySnapshot|data class SourceLocator|data class OptionalIdeCapability|object CanonicalDiscoveryJson|enum class ModuleRole' plugin/src/main/kotlin/org/jmixworkbench/discovery/model` finds all contracts; `! rg -n 'navigationId|PsiElement|VirtualFile|absolutePath' plugin/src/main/kotlin/org/jmixworkbench/discovery/model` exits 0; tests cover present/enabled/version, missing, conflicting, permutation-stable serialization, and exit 0.</acceptance_criteria>
  <done>All downstream plans share deterministic, host-neutral contracts with a safe revision-bound source locator.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Untrusted facts → canonical model | Names, paths, ordering, and conflicting values are repository-controlled. |
| Core tests → host builds | Platform leakage would defeat fast independent verification. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T2-PATH-01 | Information disclosure | `SourceLocator` | mitigate | Relative locator/fingerprint only; reflection tests reject paths and IDE types. |
| T2-SPOOF-01 | Spoofing | identity helpers | mitigate | Canonical field set, sorted encoding, and permutation tests. |
| T2-DOS-01 | Denial of service | pure lane | mitigate | No IDE distribution resolution and a measured 60-second warm ceiling. |
</threat_model>

<verification>Run `cd plugin &amp;&amp; ./gradlew phase2CoreTest --dependency-verification=strict --no-daemon` and `git diff --check`.</verification>
<success_criteria>
- Core model/fingerprint/serialization tests pass without IntelliJ dependencies.
- Safe source locators are serializable and contain no runtime navigation authority.
- Unknown and conflicting facts remain explicit and deterministic.
</success_criteria>
<output>After completion, create `.planning/phases/02-compatibility-laboratory-and-read-only-onboarding/02-01-SUMMARY.md`</output>
