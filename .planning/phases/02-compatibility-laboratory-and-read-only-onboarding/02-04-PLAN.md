---
phase: 02-compatibility-laboratory-and-read-only-onboarding
plan: "04"
type: execute
wave: 4
depends_on: ["02-02", "02-03"]
files_modified:
  - plugin/build.gradle.kts
  - plugin/hosts/idea253/build.gradle.kts
  - plugin/hosts/idea262/build.gradle.kts
  - plugin/buildSrc/src/main/java/org/jmixworkbench/build/VerifyDiscoveryEffectsTask.java
  - plugin/buildSrc/src/test/java/org/jmixworkbench/build/VerifyDiscoveryEffectsTaskTest.java
autonomous: true
requirements: [DISC-01, TEAM-07]
must_haves:
  truths:
    - "The Wave 0 source/bytecode scan rejects direct ProcessBuilder, socket, JDBC, file-store, write-action, VFS mutation, sync, or Tooling API dependencies in shared and host production discovery code; the full bridge/tool-window/service/action/ZIP closure activates after legacy write paths are removed."
    - "`phase2FastCheck` runs platform-independent tests first, intentional trip tests second, then exact 253/262 discovery host tests."
    - "The forbidden-effect scanner is itself proven by an intentional bypass fixture."
  artifacts:
    - path: "plugin/buildSrc/src/main/java/org/jmixworkbench/build/VerifyDiscoveryEffectsTask.java"
      provides: "Transitive source and compiled constant-pool forbidden effect scan"
      contains: "class VerifyDiscoveryEffectsTask"
    - path: "plugin/build.gradle.kts"
      provides: "Warm strict Wave 0 and dual-host lifecycle aggregation"
      contains: "phase2FastCheck"
  key_links:
    - from: "plugin/build.gradle.kts"
      to: "plugin/hosts/idea253/build.gradle.kts"
      via: "included-build phase2HostFastTest"
      pattern: "phase2HostFastTest"
---

<objective>
Finish Wave 0 with bypass detection and a strict fast lifecycle task used by every later implementation task.

Purpose: Production effect interfaces are enforceable only if direct imports/bytecode references fail the build.
Output: Tested forbidden-effect scanner plus `phase2FastCheck` spanning pure, tripwire, and both host lanes.
</objective>
<execution_context>
@/Users/a.k.mmuhibullahnayem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a.k.mmuhibullahnayem/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@plugin/build.gradle.kts
@plugin/hosts/idea253/build.gradle.kts
@plugin/hosts/idea262/build.gradle.kts
@plugin/src/main/kotlin/org/jmixworkbench/discovery/safety/DiscoveryEffects.kt
@.planning/phases/02-compatibility-laboratory-and-read-only-onboarding/02-VALIDATION.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Reject transitive forbidden effects and direct bypasses</name>
  <files>plugin/buildSrc/src/main/java/org/jmixworkbench/build/VerifyDiscoveryEffectsTask.java, plugin/buildSrc/src/test/java/org/jmixworkbench/build/VerifyDiscoveryEffectsTaskTest.java, plugin/build.gradle.kts</files>
  <read_first>plugin/buildSrc/src/main/java/org/jmixworkbench/build/VerifyPluginZipContentsTask.java, plugin/buildSrc/src/test/java/org/jmixworkbench/build/VerifyPluginZipContentsTaskTest.java, plugin/build.gradle.kts, plugin/src/main/kotlin/org/jmixworkbench/discovery/safety/DiscoveryEffects.kt</read_first>
  <behavior>
    - Phase A scans all shared and idea253/idea262 production `discovery/**` sources plus their compiled class constant pools/dependency edges.
    - Forbidden signatures include Gradle Tooling/sync/task/wrapper, process/runtime exec, socket/URL/HTTP, JDBC/DataSource, file-store client, IntelliJ write command/action, VFS save/refresh/write, and generator/CodeGenerationService.
    - An intentional synthetic bypass for every category is rejected; allowlisted `DiscoveryEffects` interfaces themselves do not fail.
  </behavior>
  <action>Create a cacheable `VerifyDiscoveryEffectsTask` accepting exact source/class directories, package-scope selectors, ZIP inputs, and an allowlist limited to deny-only interface declarations. Scan imports, qualified strings, compiled constant pools, and transitive class references. Register Phase A `phase2ForbiddenEffectsCheck` over shared and exact-host `org/jmixworkbench/discovery/**` production source/classes only, because the pre-existing bridge/service/action mutation path is removed in Plans 09-10; retain dormant tested rules for generator/`CodeGenerationService`/IDE service annotations so Plan 10 can expand the same task to the entire packaged closure. Test each forbidden category with synthetic source/class fixtures so removal of any rule fails tests.</action>
  <verify><automated>cd plugin &amp;&amp; ./gradlew -p buildSrc test --tests 'org.jmixworkbench.build.VerifyDiscoveryEffectsTaskTest' --dependency-verification=strict --no-daemon &amp;&amp; ./gradlew phase2ForbiddenEffectsCheck phase2FastCheck --dependency-verification=strict --no-daemon</automated></verify>
  <acceptance_criteria>`rg -n 'ProcessBuilder|GradleConnector|DriverManager|WriteCommandAction|runWriteAction|refreshAndFindFileBy' plugin/buildSrc/src/main/java/org/jmixworkbench/build/VerifyDiscoveryEffectsTask.java` finds representative rules; intentional bypass tests and the production scan exit 0.</acceptance_criteria>
  <done>Direct or transitive production bypass of the effect boundary fails before host execution.</done>
</task>

<task type="auto">
  <name>Task 2: Register pure-first dual-host phase2FastCheck</name>
  <files>plugin/build.gradle.kts, plugin/hosts/idea253/build.gradle.kts, plugin/hosts/idea262/build.gradle.kts</files>
  <read_first>plugin/build.gradle.kts, plugin/hosts/idea253/build.gradle.kts, plugin/hosts/idea262/build.gradle.kts, .planning/phases/02-compatibility-laboratory-and-read-only-onboarding/02-VALIDATION.md</read_first>
  <action>In each exact host build register `phase2HostFastTest` filtered to `org.jmixworkbench.discovery.*`. In the aggregate register ordered `phase2FastCheck`: `phase2CoreTest`, buildSrc scanner tests, `phase2ForbiddenEffectsCheck`, intentional mutation-trip tests, then idea253 and idea262 `phase2HostFastTest`. Preserve `--dependency-verification=strict`, no watch mode, and a measured warm ceiling of 120 seconds; a host task must never run if the pure lane fails.</action>
  <verify><automated>cd plugin &amp;&amp; ./gradlew phase2FastCheck --dependency-verification=strict --no-daemon</automated></verify>
  <acceptance_criteria>`./gradlew tasks --all | rg 'phase2CoreTest|phase2ForbiddenEffectsCheck|phase2FastCheck|phase2HostFastTest'` finds all tasks; build output reports pure tests before both exact host lanes; `phase2FastCheck` exits 0.</acceptance_criteria>
  <done>Wave 0 is complete and every later task has one strict focused-plus-fast feedback contract.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Production source/bytecode → build gate | Aliases or transitive references could hide a prohibited capability. |
| Aggregate lifecycle → host builds | Ordering mistakes could run expensive hosts before pure failures. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T2-BYPASS-04 | Elevation of privilege | forbidden scanner | mitigate | Source plus constant-pool/transitive scan and intentional negative fixtures. |
| T2-ORDER-04 | Denial of service | fast task | mitigate | Explicit pure-first task dependencies and 120-second warm budget. |
| T2-TAMPER-04 | Tampering | allowlist | mitigate | Exact allowlist limited to interface declarations, covered by tests. |
</threat_model>
<verification>Run `phase2FastCheck`, inspect task order, and run `git diff --check`.</verification>
<success_criteria>
- Intentional direct/transitive bypass fixtures fail the scanner for every channel.
- Clean shared and both host production sources pass.
- `phase2FastCheck` runs pure-first and both exact lanes within the warm budget.
</success_criteria>
<output>After completion, create `.planning/phases/02-compatibility-laboratory-and-read-only-onboarding/02-04-SUMMARY.md`</output>
