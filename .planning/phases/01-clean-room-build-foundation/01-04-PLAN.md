---
phase: 01-clean-room-build-foundation
plan: "04"
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - plugin/gradle/verification-metadata.xml
  - plugin/gradle/dependency-locks/
  - plugin/hosts/idea253/gradle/
  - plugin/hosts/idea262/gradle/
  - plugin/build.gradle.kts
  - .github/workflows/ci.yml
  - .github/dependabot.yml
  - README.md
  - docs/BUILDING.md
  - docs/COMPATIBILITY.md
  - docs/RELEASE-INTEGRITY.md
  - .planning/phases/01-clean-room-build-foundation/01-VALIDATION.md
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03, PROD-06]
user_setup: []
must_haves:
  truths:
    - "Resolved Gradle/npm build inputs are pinned and integrity checked."
    - "CI uses only the wrapper and project-local Node path."
    - "Documentation distinguishes IntelliJ host JVMs from target Jmix project Java 17/21/25 support."
    - "The full clean Phase 1 gate either passes or reports exact unresolved external verification evidence."
  artifacts:
    - "Reviewed Gradle dependency verification metadata and lock state exist."
    - "CI runs identity, frontend, both host builds/tests, ZIP assertions, and verifier-compatible checks."
    - "BUILDING and COMPATIBILITY documents describe exact commands/artifacts/matrices."
    - "01-VALIDATION.md records actual evidence and Nyquist status."
  key_links:
    - "CI invokes `plugin/gradlew phase1Check`, not global Gradle/npm."
    - "Documentation artifact names match built lane-suffixed ZIPs."
---

<objective>
Lock supply-chain inputs, automate the Phase 1 gate, and publish exact build/compatibility evidence.

Purpose: Make the foundation repeatable for contributors and CI, with claims limited to what the artifacts prove.
Output: Dependency verification/locks, CI, build/compatibility docs, and completed validation record.
</objective>

<execution_context>
@/Users/a.k.mmuhibullahnayem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a.k.mmuhibullahnayem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-clean-room-build-foundation/01-03-SUMMARY.md
@.planning/phases/01-clean-room-build-foundation/01-RESEARCH.md
@.planning/phases/01-clean-room-build-foundation/01-VALIDATION.md
@plugin/gradle/libs.versions.toml
@plugin/gradle/wrapper/gradle-wrapper.properties
@webui/package-lock.json
@README.md
</context>

<threat_model>
- T1-SUPPLY: compromised/dynamic dependencies or wrapper enter builds. Require exact versions, wrapper checksum, lock state, and reviewed SHA-256 verification metadata.
- T1-CI: CI silently uses globally available Node/Gradle or mutable action tags. Invoke wrapper only, project-local Node tasks, and pin actions to immutable commit SHAs with version comments.
- T1-SECRET: future signing inputs leak into repository/log/artifacts. Add secret-name interfaces/documentation only; never add values and scan artifacts/source.
- T1-CLAIM: docs imply all Java/Jmix versions or generators are certified. Publish exact host lanes and planned target-project cells, with mutation explicitly disabled/non-certified.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Lock and verify build dependencies and repositories</name>
  <files>plugin/gradle/verification-metadata.xml, plugin/gradle/dependency-locks/**, plugin/hosts/idea253/gradle/**, plugin/hosts/idea262/gradle/**, plugin/build.gradle.kts, plugin/settings.gradle.kts</files>
  <read_first>plugin/settings.gradle.kts, plugin/build.gradle.kts, plugin/hosts/idea253/build.gradle.kts, plugin/hosts/idea262/build.gradle.kts, plugin/gradle/libs.versions.toml, webui/package-lock.json, .planning/phases/01-clean-room-build-foundation/01-RESEARCH.md</read_first>
  <action>Restrict dependency repositories to Gradle Plugin Portal, Maven Central, and the documented IntelliJ Platform default repositories required by the two lane builds. In each included build, activate dependency locking only for the explicit standard configurations `runtimeClasspath` and `testRuntimeClasspath`; do not lock detached, IDE archive, instrumentation, or verifier-target configurations. During implementation only, generate those four lock states with scoped `dependencies --configuration <name> --write-locks` commands, generate SHA-256 dependency-verification metadata while resolving the successful Phase 1 tasks, and manually review the resulting diff for unexpected repositories/components. Final verification must be read-only: resolve `runtimeClasspath` and `testRuntimeClasspath` without `--write-locks` under strict dependency verification. Keep trusted-key use only where publisher signatures are actually available. Add `verifyDependencyIntegrity` to fail on dynamic/changing versions, missing wrapper checksum, mismatched wrapper JAR checksum, missing npm lockfile, package-lock drift (`npm ci`), or dependency-verification bypass flags in CI configuration. Do not weaken verification to make transient failures green.</action>
  <verify><automated>cd plugin && ./gradlew snapshotLockHashes verifyLockedConfigurations --dependency-verification=strict verifyDependencyIntegrity phase1FastCheck compareLockHashes --non-interactive</automated></verify>
  <acceptance_criteria>
    - `gradle/verification-metadata.xml` exists and contains SHA-256 checksums.
    - Lock state exists for both host builds or a documented Gradle limitation is enforced by exact version and verification metadata instead.
    - Build declarations contain no dynamic/changing versions.
    - `verifyDependencyIntegrity` checks wrapper distribution/JAR checksums and npm lockfile.
    - `snapshotLockHashes` records SHA-256 values of the intended lock files before read-only resolution and `compareLockHashes` proves the values are unchanged afterward.
    - Strict dependency verification command exits 0.
  </acceptance_criteria>
  <done>Every resolved Phase 1 build input is exact, locked where supported, and integrity verified.</done>
</task>

<task type="auto">
  <name>Task 2: Add immutable CI, exact build docs, and close the Phase 1 validation record</name>
  <files>.github/workflows/ci.yml, .github/dependabot.yml, README.md, docs/BUILDING.md, docs/COMPATIBILITY.md, docs/RELEASE-INTEGRITY.md, .planning/phases/01-clean-room-build-foundation/01-VALIDATION.md</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-clean-room-build-foundation/01-VALIDATION.md, plugin/build.gradle.kts, plugin/gradle/libs.versions.toml, README.md, SECURITY.md</read_first>
  <action>Add GitHub Actions CI with immutable action commit SHAs (retain version comments) that checks out source, installs one Java 21 bootstrap JDK for Gradle, validates wrapper checksum, and lets Foojay resolver 1.0.0 auto-provision the exact Java 25 compilation toolchain; then run `cd plugin && ./gradlew clean phase1Check --non-interactive --dependency-verification=strict`. Do not install global Node or Gradle. `verifyHostToolchains` must assert the actual compiler launchers are Java 21/25. Upload both ZIPs and verification/test reports as non-release artifacts. Add Dependabot for Gradle, npm, and GitHub Actions with review-only update PRs. Write `docs/BUILDING.md` with the wrapper-only command, project-local Node 24.18.0 behavior, Foojay-provisioned JDK 21/25 toolchains, first-run/offline behavior, lane artifacts, and no-global-Node reproduction. Write `docs/COMPATIBILITY.md` with host matrix (253–261/JBR21, 262/JBR25), and separate target-project plan (Jmix 2.8 Java17/21, Jmix3 Java21/25; newer officially compatible JDKs read-only until certified). Write `docs/RELEASE-INTEGRITY.md` describing locks, verification, checksums, future signing/SBOM interfaces without claiming Phase 8 completion. Update README to link these docs and label all repository mutation features non-certified/disabled for valuable repositories. Run the full clean gate, record actual automated commands/results in `01-VALIDATION.md`, set `nyquist_compliant: true` and `wave_0_complete: true` only when automated rows are green, but keep `Approval: pending manual installation` until a real installation checkpoint occurs. Plugin Verifier is automated and must pass; manual IDE installation remains a separate pending row if not performed.</action>
  <verify><automated>cd plugin && ./gradlew clean phase1Check --non-interactive --dependency-verification=strict && cd .. && rg -n "Java 17|Java 21|Java 25|Node 24\\.18\\.0|project-local|non-certified" docs/BUILDING.md docs/COMPATIBILITY.md README.md && rg -n "nyquist_compliant: true|wave_0_complete: true|Approval: pending manual installation" .planning/phases/01-clean-room-build-foundation/01-VALIDATION.md</automated></verify>
  <acceptance_criteria>
    - CI invokes `./gradlew`, not `gradle`, `npm install`, or a global Node setup action.
    - GitHub Actions `uses:` entries are immutable full commit SHAs with readable version comments.
    - CI uploads both lane ZIPs and test/verification reports.
    - BUILDING documents a no-global-Node wrapper build.
    - COMPATIBILITY separates host JBR requirements from target-project Java 17/21/25 cells and does not claim untested future support.
    - Automated Nyquist status is green only when all automated rows and the full clean command pass; overall approval remains pending until manual installation evidence exists.
  </acceptance_criteria>
  <done>Phase 1 is reproducible in CI, its dependencies are verified, and its documentation/validation claims match actual evidence.</done>
</task>

</tasks>

<verification>
- [ ] Strict dependency verification passes.
- [ ] CI is wrapper-only and project-local-Node-only.
- [ ] Full clean Phase 1 gate passes.
- [ ] Built artifact names/checksums and documentation agree.
- [ ] Validation status contains no false green/manual evidence.
</verification>

<success_criteria>
- PROD-01, PROD-02, PROD-03, and PROD-06 are evidenced without workstation-global tooling.
- Supply-chain integrity is enforced, not merely documented.
- Java 17-through-latest policy is exact and version-aware.
- Phase 1 completion status reflects real automated/manual evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/01-clean-room-build-foundation/01-04-SUMMARY.md`.
</output>
