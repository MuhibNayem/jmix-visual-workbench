---
phase: 01-clean-room-build-foundation
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - plugin/src/main/kotlin/org/jmixworkbench/generator/ViewXmlGenerator.kt
  - plugin/src/main/kotlin/org/jmixworkbench/toolwindow/JmixWorkbenchToolWindowFactory.kt
  - plugin/src/test/kotlin/org/jmixworkbench/
  - plugin/hosts/idea253/src/test/
  - plugin/hosts/idea262/src/test/
  - plugin/buildSrc/
autonomous: true
requirements: [PROD-02, PROD-03]
user_setup: []
must_haves:
  truths:
    - "Both host lanes compile and produce installable ZIPs from the same source."
    - "Packaged web resources resolve; missing resources and unsupported JCEF show explicit safe fallback UI."
    - "Only compile/package blockers are fixed; unsafe prototype generation is not certified."
  artifacts:
    - "Both host distribution ZIPs exist with lane suffixes."
    - "Platform/resource smoke tests exist for each lane."
    - "ZIP content assertions prove the same-revision web bundle is present and Node caches are absent."
  key_links:
    - "Tool-window factory loads the packaged web entry point used by ZIP content tests."
    - "Aggregate hostSmokeTest/buildHostPlugins tasks depend on both lane tasks."
---

<objective>
Make shared source compile on both host lanes and prove packaged plugin/resource behavior.

Purpose: Turn build configuration into installable evidence without pretending the prototype generators are production-safe.
Output: Compile fixes, host smoke tests, ZIP assertions, and two built distributions.
</objective>

<execution_context>
@/Users/a.k.mmuhibullahnayem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a.k.mmuhibullahnayem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-clean-room-build-foundation/01-02-SUMMARY.md
@.planning/phases/01-clean-room-build-foundation/01-RESEARCH.md
@.planning/phases/01-clean-room-build-foundation/01-VALIDATION.md
@plugin/src/main/kotlin/org/jmixworkbench/generator/ViewXmlGenerator.kt
@plugin/src/main/kotlin/org/jmixworkbench/toolwindow/JmixWorkbenchToolWindowFactory.kt
@plugin/src/main/resources/META-INF/plugin.xml
</context>

<threat_model>
- T1-HIDDEN: compilation errors are suppressed by excluding unsafe source. Compile all shared runtime source; fix only concrete blockers.
- T1-RESOURCE: a ZIP builds but packaged UI is absent or stale. Assert entry point, hashed assets, and build manifest in both ZIPs.
- T1-JCEF: unsupported JCEF yields blank content or local filesystem fallback. Test an explicit non-privileged error panel and remove production fallback to checkout paths.
- T1-CLAIM: successful packaging is mistaken for generator certification. Keep README/status and UI warnings explicitly non-certified.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Resolve shared-source compile blockers and harden packaged resource fallback</name>
  <files>plugin/src/main/kotlin/org/jmixworkbench/generator/ViewXmlGenerator.kt, plugin/src/main/kotlin/org/jmixworkbench/toolwindow/JmixWorkbenchToolWindowFactory.kt, plugin/src/test/kotlin/org/jmixworkbench/**</files>
  <read_first>plugin/src/main/kotlin/org/jmixworkbench/generator/ViewXmlGenerator.kt, plugin/src/main/kotlin/org/jmixworkbench/generator/XmlBuilder.kt, plugin/src/main/kotlin/org/jmixworkbench/toolwindow/JmixWorkbenchToolWindowFactory.kt, .planning/codebase/CONCERNS.md</read_first>
  <action>Compile all shared source for both lanes and fix each real compiler error without broad generator refactors. At minimum, make the DataGrid helper name consistent (`generateDataGridContents`) and call child creation through its `parent` receiver. In the tool-window factory, remove the checkout-relative `File("webui/dist/index.html")` production fallback; resolve the packaged entry point through a single testable resource resolver. When JCEF is unsupported or the packaged resource is missing, render an explicit Swing error panel with stable diagnostic code (`JVW-JCEF-UNAVAILABLE` or `JVW-WEB-BUNDLE-MISSING`) and no file/network access. Add focused tests for resolver success/missing behavior and fallback message creation. Do not change direct project-write behavior in this phase except to keep it non-certified/unadvertised.</action>
  <verify><automated>cd plugin && ./gradlew compileHostKotlin testShared hostSmokeTest --non-interactive</automated></verify>
  <acceptance_criteria>
    - Repository contains one `generateDataGridContents` declaration and matching call.
    - DataGrid helper uses `parent.child("columns")`.
    - Tool-window production code does not contain `File("webui/dist` or return `about:blank`.
    - Stable codes `JVW-JCEF-UNAVAILABLE` and `JVW-WEB-BUNDLE-MISSING` are tested.
    - Both lane Kotlin compilation tasks exit 0.
  </acceptance_criteria>
  <done>Shared code compiles for both targets and packaged-resource failure is explicit, local, and test-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble and inspect both plugin ZIPs with host smoke evidence</name>
  <files>plugin/hosts/idea253/src/test/**, plugin/hosts/idea262/src/test/**, plugin/buildSrc/**, plugin/build.gradle.kts</files>
  <read_first>plugin/hosts/idea253/build.gradle.kts, plugin/hosts/idea262/build.gradle.kts, plugin/src/main/resources/META-INF/plugin.xml, plugin/build/generated-resources/webui/build-info.json, .planning/phases/01-clean-room-build-foundation/01-VALIDATION.md</read_first>
  <action>Add lane-specific platform smoke tests that load/validate the plugin descriptor, instantiate or exercise the tool-window resource/fallback seam without mutating a project, and assert plugin ID/name/build range/JCEF dependency. Configure each included host build's IntelliJ Platform `pluginVerification` block with the exact minimum IDE: IDEA 2025.3 for idea253 and IDEA 2026.2 for idea262; expose included-build `verifyPlugin` tasks through root `verifyHostPlugins`. Add `verifyPluginZipContents` to inspect both built archives: require plugin JAR/descriptor, `webui/index.html`, referenced hashed assets, `webui/build-info.json`, original icon, `LICENSE`, and `NOTICE`; reject `node_modules`, `.npm`, downloaded Node, absolute developer paths, old product/package strings, and unapproved source maps. Wire `buildHostPlugins`, `hostSmokeTest`, `verifyHostPlugins`, and `verifyPluginZipContents` into `phase1Check`. Produce deterministic lane-suffixed ZIP names and print their paths/checksums.</action>
  <verify><automated>cd plugin && ./gradlew clean buildHostPlugins hostSmokeTest verifyHostPlugins verifyPluginZipContents --non-interactive && find . -path '*/build/distributions/*.zip' -print</automated></verify>
  <acceptance_criteria>
    - Exactly one idea253 and one idea262 distribution ZIP are produced.
    - Both ZIP content checks pass and contain the same `build-info.json` input digest.
    - idea253 smoke assertion expects build range `253`–`261.*`; idea262 expects `262`–`262.*` and explicit JCEF dependency.
    - ZIP inspection rejects Node/npm caches and old `com.jmixstudio`/`Jmix Studio Clone` strings.
    - Plugin Verifier passes idea253 against IDEA 2025.3 and idea262 against IDEA 2026.2.
    - Aggregate command exits 0.
  </acceptance_criteria>
  <done>Both honest host artifacts assemble and pass resource, descriptor, and non-mutating load smoke gates.</done>
</task>

</tasks>

<verification>
- [ ] Both lane compile/test/build tasks pass.
- [ ] Resource fallback tests pass.
- [ ] ZIP allowlist/denylist checks pass for both artifacts.
- [ ] No Jmix repository file is written during smoke tests.
</verification>

<success_criteria>
- PROD-02 build/install preconditions are automated.
- PROD-03 packaged-resource guarantees are demonstrated in both ZIPs.
- Known compile blockers are resolved without false feature certification.
</success_criteria>

<output>
After completion, create `.planning/phases/01-clean-room-build-foundation/01-03-SUMMARY.md`.
</output>
