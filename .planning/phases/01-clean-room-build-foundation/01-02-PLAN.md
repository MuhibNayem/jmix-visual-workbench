---
phase: 01-clean-room-build-foundation
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - plugin/settings.gradle.kts
  - plugin/build.gradle.kts
  - plugin/gradle.properties
  - plugin/gradle/libs.versions.toml
  - plugin/gradle/wrapper/gradle-wrapper.properties
  - plugin/gradle/wrapper/gradle-wrapper.jar
  - plugin/gradlew
  - plugin/gradlew.bat
  - plugin/hosts/idea253/
  - plugin/hosts/idea262/
  - plugin/buildSrc/
  - webui/package.json
  - webui/package-lock.json
autonomous: true
requirements: [PROD-01, PROD-03]
user_setup: []
must_haves:
  truths:
    - "The checked-in wrapper is the only required Gradle entry point."
    - "A global Node installation is not required to build, and an installed plugin never requires Node."
    - "IDE 253-261 and IDE 262 artifacts are built from one canonical source tree with correct JVM/JCEF contracts."
    - "Plugin packaging cannot reuse stale checked-out webui/dist output."
  artifacts:
    - "Gradle 9.5.1 wrapper scripts, JAR, distribution URL, and SHA-256 checksum are checked in."
    - "The build provisions Node 24.18.0 with com.github.node-gradle.node 7.1.0 and download=true."
    - "idea253 and idea262 are isolated included builds using Kotlin Gradle Plugin 2.4.0 with shared canonical Kotlin/resources; idea253 is constrained to Kotlin language/API 2.2."
    - "JDK 21 and 25 compilation toolchains auto-provision through the pinned Foojay resolver when absent."
    - "A build-owned web resource manifest fingerprints declared UI inputs."
  key_links:
    - "Host processResources/buildPlugin tasks depend on the Gradle-owned web bundle task."
    - "Aggregate buildHostPlugins invokes both lane builds."
---

<objective>
Create the complete self-sustaining dual-lane Gradle build and same-revision frontend resource pipeline.

Purpose: Make clean builds independent of global Gradle/Node and prevent stale UI or false host compatibility.
Output: Complete wrapper, pinned build/toolchains, two host artifacts, and Gradle-owned web packaging.
</objective>

<execution_context>
@/Users/a.k.mmuhibullahnayem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/a.k.mmuhibullahnayem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-clean-room-build-foundation/01-01-SUMMARY.md
@.planning/phases/01-clean-room-build-foundation/01-RESEARCH.md
@.planning/phases/01-clean-room-build-foundation/01-VALIDATION.md
@plugin/build.gradle.kts
@plugin/settings.gradle.kts
@plugin/gradle.properties
@plugin/gradle/wrapper/gradle-wrapper.properties
@webui/package.json
@webui/package-lock.json
</context>

<threat_model>
- T1-BUILD: developer-global Gradle/Node/JDK changes results. Use wrapper, exact toolchains, and downloaded project-local Node.
- T1-STALE: pre-existing `webui/dist` is packaged. Build only from a Gradle-owned output directory with declared inputs and a digest manifest.
- T1-HOST: one descriptor/binary claims incompatible IDE/JBR/JCEF ranges. Build separate lane modules with exact build ranges and dependencies.
- T1-DOWNLOAD: unpinned tooling is downloaded. Pin Gradle 9.5.1/checksum, Foojay resolver 1.0.0, Node 24.18.0, Node Gradle plugin 7.1.0, IntelliJ Platform Gradle plugin 2.18.0, and lane Kotlin/IDE versions.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Restore and pin the Gradle wrapper and build version sources</name>
  <files>plugin/gradlew, plugin/gradlew.bat, plugin/gradle/wrapper/gradle-wrapper.jar, plugin/gradle/wrapper/gradle-wrapper.properties, plugin/gradle/libs.versions.toml, plugin/gradle.properties, plugin/settings.gradle.kts</files>
  <read_first>plugin/settings.gradle.kts, plugin/build.gradle.kts, plugin/gradle.properties, plugin/gradle/wrapper/gradle-wrapper.properties, .planning/phases/01-clean-room-build-foundation/01-RESEARCH.md</read_first>
  <action>Generate the complete Gradle 9.5.1 wrapper, check in launcher scripts and wrapper JAR, set `distributionUrl=https\\://services.gradle.org/distributions/gradle-9.5.1-bin.zip`, `distributionSha256Sum=bafc141b619ad6350fd975fc903156dd5c151998cc8b058e8c1044ab5f7b031f`, and validate wrapper JAR SHA-256 `497c8c2a7e5031f6aa847f88104aa80a93532ec32ee17bdb8d1d2f67a194a9c7`. Make `gradlew` executable. Add a version catalog or root build constants containing exact versions: IntelliJ Platform Gradle Plugin `2.18.0`, Foojay resolver `1.0.0`, Node Gradle plugin `7.1.0`, Node `24.18.0`, Gson `2.11.0`, Kotlin Gradle Plugin `2.4.0` for both included builds, idea253 Kotlin language/API floor `2.2`, minimum target IDE `2025.3`, and 262 target `2026.2`. Configure plugin repositories/default IntelliJ repositories without dynamic versions. Keep root project name `jmix-visual-workbench`.</action>
  <verify><automated>cd plugin && ./gradlew --version --non-interactive && shasum -a 256 gradle/wrapper/gradle-wrapper.jar && rg -n "gradle-9\\.5\\.1-bin|bafc141b|2\\.18\\.0|1\\.0\\.0|24\\.18\\.0|7\\.1\\.0|jmix-visual-workbench" gradle/wrapper/gradle-wrapper.properties gradle/libs.versions.toml settings.gradle.kts</automated></verify>
  <acceptance_criteria>
    - `plugin/gradlew`, `plugin/gradlew.bat`, and `plugin/gradle/wrapper/gradle-wrapper.jar` exist.
    - Wrapper version output contains `Gradle 9.5.1`.
    - Wrapper distribution and JAR checksums equal the exact official values in the action.
    - No `+`, `latest`, `SNAPSHOT`, or version range appears in build dependency declarations.
  </acceptance_criteria>
  <done>The repository has a complete, checksum-pinned, globally independent Gradle entry point.</done>
</task>

<task type="auto">
  <name>Task 2: Build same-revision web resources and two host-specific plugin lanes</name>
  <files>plugin/build.gradle.kts, plugin/settings.gradle.kts, plugin/hosts/idea253/**, plugin/hosts/idea262/**, plugin/buildSrc/**, webui/package.json, webui/package-lock.json</files>
  <read_first>plugin/build.gradle.kts, plugin/settings.gradle.kts, plugin/gradle/libs.versions.toml, plugin/src/main/resources/META-INF/plugin.xml, plugin/src/main/kotlin/org/jmixworkbench/toolwindow/JmixWorkbenchToolWindowFactory.kt, webui/package.json, webui/package-lock.json, webui/vite.config.ts</read_first>
  <action>Turn `plugin/` into an aggregate build that applies neither Kotlin nor IntelliJ plugins. Add `hosts/idea253` and `hosts/idea262` as isolated included builds, each with its own `settings.gradle.kts`, IntelliJ Platform dependency, `org.gradle.toolchains.foojay-resolver-convention` 1.0.0, Kotlin Gradle Plugin 2.4.0, and source sets pointing to the single canonical `plugin/src/main/kotlin` and shared resources. Configure idea253 against IntelliJ 2025.3, JVM/toolchain 21, Kotlin compiler `languageVersion=2.2` and `apiVersion=2.2`, `sinceBuild=253`, `untilBuild=261.*`; configure idea262 against IntelliJ 2026.2, JVM/toolchain 25, Kotlin language/API 2.4, `sinceBuild=262`, `untilBuild=262.*`, and the documented explicit JCEF module/dependency. Disable Kotlin's default stdlib dependency and rely on the host-bundled runtime. Add a build assertion that idea253 compiler options are exactly Kotlin 2.2/JVM21 and that neither plugin ZIP bundles kotlin-stdlib/coroutines. Assert compiler launcher metadata reports Java language version 21/25 respectively. Use plugin ID/version from one shared provider and lane-suffixed distribution filenames. Apply Node Gradle plugin 7.1.0 at the aggregate root, set Node `version=24.18.0`, `download=true`, `nodeProjectDir=../webui`, and define `npmCi` plus `buildWebUi` using npm `ci` then the existing build script. Declare all UI sources/config/lockfile as inputs; copy outputs into `plugin/build/generated-resources/webui`; generate `build-info.json` containing plugin version, Git revision when available, and a SHA-256 digest of declared inputs. Make included host resource/package tasks consume only that verified output. Register root aggregate tasks through `gradle.includedBuild("idea253").task(...)` and `gradle.includedBuild("idea262").task(...)` for compile/build/test/verifier lifecycles. Installed code must resolve only packaged resources in production and must not launch Node.</action>
  <verify><automated>cd plugin && env PATH="/usr/bin:/bin:/usr/sbin:/sbin" ./gradlew npmSetup buildWebUi verifyWebBundle verifyHostToolchains phase1FastCheck --non-interactive && test -f build/generated-resources/webui/index.html && test -f build/generated-resources/webui/build-info.json</automated></verify>
  <acceptance_criteria>
    - Build configuration contains Node `24.18.0`, `download.set(true)`, and npm command `ci`.
    - `buildWebUi` output is under `plugin/build/`, not `webui/dist`.
    - Both lane package/resource tasks depend on `verifyWebBundle`.
    - idea253 descriptor/build values are exactly 253 through 261.* and JVM 21.
    - idea262 descriptor/build values are exactly 262 through 262.*, JVM 25, and explicit JCEF dependency.
    - Each host applies Kotlin Gradle Plugin 2.4.0 and Foojay resolver 1.0.0; idea253 compiler language/API is exactly 2.2 and the aggregate build applies no Kotlin plugin.
    - Neither plugin artifact bundles kotlin-stdlib or kotlinx-coroutines.
    - `verifyHostToolchains` proves idea253 uses Java 21 and idea262 uses Java 25, auto-provisioning missing JDKs.
    - The no-global-Node verification command exits 0 and generates `index.html` plus `build-info.json`.
  </acceptance_criteria>
  <done>One wrapper command provisions Node, builds same-revision assets, and prepares both honest host-specific distributions.</done>
</task>

</tasks>

<verification>
- [ ] Wrapper version/checksums pass.
- [ ] `phase1FastCheck` passes with Node removed from PATH.
- [ ] Generated web bundle and input manifest are build-owned.
- [ ] Both host build definitions consume the same canonical source and correct JVM/JCEF contracts.
</verification>

<success_criteria>
- PROD-01 and PROD-03 have automated evidence.
- End users have no Node runtime dependency.
- No stale checked-out UI output can enter a plugin ZIP.
</success_criteria>

<output>
After completion, create `.planning/phases/01-clean-room-build-foundation/01-02-SUMMARY.md`.
</output>
