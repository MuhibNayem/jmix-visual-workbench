# Jmix Studio Surpass Ledger

Baseline date: 2026-07-29

This is the binding completion ledger for the product goal: build a clean-room,
source-safe IntelliJ plugin that reaches documented Jmix Studio parity and then
surpasses it for large enterprise Jmix development.

It supplements, and does not replace:

- `FULL-GUI-DEVELOPMENT-REQUIREMENTS.md`;
- `ENTERPRISE-PARITY-AUDIT.md`;
- the supplied 16-module payroll architecture and supervisor requirements; and
- the supplied Studio comparison dated 2026-07-29.

No feature may be called complete merely because a screen exists. Every item in
this ledger is governed by the per-feature definition of done in
`FULL-GUI-DEVELOPMENT-REQUIREMENTS.md`: discovery, typed representation,
validation, impact analysis, immutable diff preview, atomic apply and undo,
round-trip safety, target-toolchain verification, runtime inspection,
accessibility, documentation, and enterprise-scale tests.

## Status and claim rules

| Status | Meaning |
|---|---|
| `STRONG` | Implemented and verified in the current audit, with only explicitly listed hardening work remaining |
| `PARTIAL` | Useful implementation exists, but one or more mandatory behavior or proof gates are missing |
| `MISSING` | No production-capable implementation exists |
| `ACTIVE` | The current implementation tranche |

The product MUST NOT claim:

- **Studio breadth parity** until every `STUDIO-*` item is `STRONG`;
- **production parity** until every `CERT-*` item is `STRONG`; or
- **clear enterprise superiority** until every `SURPASS-*` item is `STRONG` on
  representative existing enterprise projects.

Percentages and broad labels are planning aids only. The atomic items and their
evidence decide completion.

## Binding product goal

The project is complete only when every `STUDIO-*`, `SURPASS-*`, and `CERT-*`
row in this ledger is `STRONG` and its stated evidence gate has passed in the
current source and installable artifacts. `PARTIAL`, `SUBSTANTIAL`, `ACTIVE`
and `MISSING` are work queues, not acceptable final states. A high aggregate
percentage, a stronger architecture in another category, or a visually present
screen cannot compensate for an incomplete row.

**Owner acceptance instruction (reaffirmed 2026-07-31): every point in this
ledger must reach `STRONG`. No row may be waived, averaged away, or treated as
optional, and the goal must remain active until the current installable
artifacts carry the required evidence for every row.**

## Official documented baseline

The clean-room baseline is derived only from public documentation:

- [Studio feature list](https://docs.jmix.io/jmix/studio/studio-features.html)
- [Using Jmix Studio](https://docs.jmix.io/jmix/studio/index.html)
- [Coding assistance](https://docs.jmix.io/jmix/studio/coding-assistance.html)
- [Entity designer](https://docs.jmix.io/jmix/studio/entity-designer.html)
- [View designer](https://docs.jmix.io/jmix/studio/view-designer.html)
- [View creation wizard](https://docs.jmix.io/jmix/studio/view-wizard.html)
- [Add-ons marketplace](https://docs.jmix.io/jmix/studio/marketplace.html)
- [Composite projects](https://docs.jmix.io/jmix/studio/composite-projects.html)

Public behavior is a compatibility target, not a license to copy proprietary
source code, assets, protocols, or internal implementation.

## Entity Designer product-gap priority

The safety engine is not the current limiting factor. The shortest path from
the present substantial implementation to a superior daily developer product
is:

1. remaining high-frequency entity actions: localized-caption workflows and
   safe enum-usage migration, plus
   evidence-complete shape migration for existing inheritance hierarchies and
   embedded overrides. Root/subtype discriminator authoring, explicit nested
   scalar/association overrides, copy/reorder, inherited/trait visibility, callback
   authoring, current Java/Kotlin Jmix listener creation/navigation and
   exact-revision entity-to-view creation/source/design navigation are
   implemented with handwritten-source protection. Typed Java/Kotlin Jmix
   repository creation, existing-source reconstruction, additive authoring,
   lossless supported metadata editing, repository-backed Java/Kotlin view
   delegates, indexed native Java/Kotlin repository injection and native
   JPQL-parameter refactoring plus exact-source Open/Rename/Change-Signature/
   Safe-Delete workflows are implemented. Aggregate update-service generation
   remains;
2. decomposition of the 6,051-line central React designer plus component, interaction,
   responsive and accessibility regression tests;
3. representative arbitrary-source, database-first, installed-IDE dual-host,
   scale, latency, memory and leak certification.

Until those items pass, advanced migration and relationship capabilities do not
justify an overall Entity Designer superiority claim.

## A. Studio core development parity

| ID | Mandatory capability | Current status | Completion evidence required |
|---|---|---|---|
| STUDIO-CORE-001 | Java and Kotlin entity designer: persistent entities, DTOs, mapped superclasses, embeddables, composite IDs, traits, inheritance, validations, lifecycle callbacks, listeners, indexes, DB views, repositories and localization | PARTIAL | The typed model/generator now covers all official scalar datatype families plus explicit custom Java/SQL types, configured embedded IDs with Java/Kotlin `Serializable` member-based value semantics, schema/catalog-qualified `@Table` mappings, correct `EnumClass<String|Integer>` ID persistence, Jmix-only DTO metadata, comments/system/LOB/property-dependency/datatype/validation-group/read-only metadata, repositories and aligned Liquibase types. Root/subtype inheritance authoring now covers all three JPA strategies, discriminator column/type/length/value metadata and JOINED PK joins without duplicating inherited ID/version/trait members. Explicit nested scalar and association embedded overrides, including multi-column joins, round-trip through Java/Kotlin generation and source parsing; exact-revision handwritten entities can add an embedded property with checked forward/rollback DDL, while unsafe existing hierarchy/override shape changes fail closed. Native Java/Kotlin selection writes entities, enums, repositories and activation configuration into the matching module source set. Existing handwritten Java and Kotlin both support revision-bound, current-document-aware, source-preserving additions and managed nullability/uniqueness/length/precision/scale updates with Kotlin PSI validation and the same atomic checked forward/rollback Liquibase plan. Stable-mapping scalar and relationship properties launch IntelliJ native usage-preview rename from the visual designer after exact-revision, collision and schema-impact checks; JPA `mappedBy` strings provide Java/Kotlin completion, navigation, indexed Find Usages and rename references, while FlowUI/fetch-plan/JPQL/security references use the plugin's native reference graph. Handwritten scalar properties request IntelliJ project-wide Type Migration through exact Java fields or Kotlin light fields; source-only, expand/contract, external-DDL and incomplete-evidence outcomes plus PK/unique/index/incoming/outgoing-FK dependencies are reported before preview, and lossy SQL rewrites are never described as automatically reversible. Source-only outcomes open the real preview. Proven lossless SQL changes create a deterministic shadow-column expansion with transactional backfill, restored nullability, schema qualification, HALT safety policies and original-data-preserving rollback; unproven conversions fail closed. Expand/contract source migration now requires an expiring backend capability created only after credential-contained live verification proves original/shadow presence, target SQL capacity and exact row-value parity. Mapping cutover rechecks the database after native Java/Kotlin Type Migration and changes only the exact `@Column(name)` literal through revision-bound atomic preview/apply. Explicit scalar and owning to-one physical-column renames update Java/Kotlin `@Column`/`@JoinColumn` mappings and generate old-exists/new-absent Liquibase preconditions plus reverse rollback atomically while preserving unmanaged annotation arguments; inferred, inverse, collection, join-table, cross-store, shape-changing, colliding and unmanaged-DDL requests fail closed. Coordinated logical/physical rename now applies the mapping and Liquibase change atomically, refreshes project evidence, then launches IntelliJ's native usage-preview rename. Established bidirectional many-to-one/one-to-many and one-to-one/one-to-one conversions now update both handwritten Java/Kotlin association declarations atomically while restricting unique-constraint DDL to the proven owning join column. Ambiguous removal/rename requests fail closed. Static attribute propagation now exceeds Studio's basic Add to Views workflow: one impact review discovers and atomically updates matching FlowUI forms/grids, inline/shared fetch plans, default entity messages and exact non-wildcard resource-role attribute policies; `_base`, self-closing XML, stale revisions, privilege opt-in, locale review and idempotency are enforced. Real Jmix Java/Kotlin entity files now open in a native IntelliJ Design editor beside source, bound to the current unsaved document revision; New Entity/View/CRUD actions route to exact workspaces through a retained project channel. Shared and both host descriptors plus nested packaged-ZIP checks enforce registration. The entity-kind-aware UI adds enum authoring, metadata controls and connected application-impact consumers. Focused contracts pass on both host lanes. Other relationship shapes, contraction, final-retention deletion, dynamic/controller and inherited/fragment propagation, composite-ID migration proof and installed-IDE polish remain; see `ENTITY-DESIGNER-PARITY.md` |
| STUDIO-CORE-002 | Data-store designer: main/additional stores, SQL and non-SQL stores, credentials as property references, module ownership and connection testing | PARTIAL | Real-store fixtures, secret redaction and atomic configuration changes |
| STUDIO-CORE-003 | View creation wizard for entity, DTO, blank, fragment and main-view templates, including repositories, fetch plans, messages and menu updates | PARTIAL | One atomic multi-file preview/apply pipeline for every template |
| STUDIO-CORE-004 | Complete FlowUI WYSIWYG designer with source/controller navigation, custom components, themes, accessibility, responsive behavior and manual-source preservation | PARTIAL | Real `view` and `fragment` descriptors now open in a native IntelliJ Design editor beside the standard XML editor on both supported hosts. The isolated private-origin surface launches by safe relative locator and current SHA-256 revision, reads unsaved manual documents, applies visual changes through IntelliJ documents when open/dirty, persists clean unopened files, rejects post-preview manual edits, participates in modified state and visual undo/redo, and reloads the current XML revision on reselection. Browser evidence at 1280/640/440/320 pixels proves the editor route has no global sidebar or horizontal shell overflow; palette, canvas and inspector remain simultaneous columns or a continuous no-tab stack, and insertion/undo execute through the safe bridge. Dual-host provider, document-round-trip, composite-root, resource-route, full-suite, smoke-test and Plugin Verifier gates pass. Complete runtime-fidelity/custom-component/theme/accessibility/hot-reload fixtures, every handwritten-controller construct and installed-IDE JCEF memory/leak proof remain |
| STUDIO-CORE-005 | Standalone fetch-plan discovery, creation, inheritance, visual editing, validation, usage navigation and safe refactoring | PARTIAL | Native shared/inline fetch-plan completion, navigation, Find Usages, cross-file rename, built-in plan support, entity/property validation, duplicate/ambiguity detection and modification-aware project caching are verified across both IDE hosts. Complete designer round trip, fetch-coverage analysis, all inheritance semantics and real multi-store runtime proof remain |
| STUDIO-CORE-006 | At least three-level menu authoring, hierarchy validation, duplicate detection, role visibility, source navigation and safe refactoring | PARTIAL | Three-level visual authoring is implemented; native nested menu-ID and Spring bean/method completion, navigation, Find Usages, safe rename and diagnostics are verified on both IDE hosts. Callable Java methods require exactly `Map<String, Object>` (or no parameter), while Kotlin requires the JVM-equivalent `Map<String, Any>` contract; raw and wrong-key/value maps fail closed. The visual inspector consumes the same indexed bean/method inventory and preserves custom configuration. Migrate every remaining menu write to immutable preview/apply and add exhaustive hierarchy/role-visibility fixtures |
| STUDIO-CORE-007 | Resource and row role authoring, inheritance, entity/attribute/view/menu/specific policies and effective-access analysis | STRONG | Visual effective-access analysis is joined by native Java/Kotlin/XML assistance for view/menu policies, entity metadata aliases, nested attribute paths, JPQL row paths and specific permissions. Remaining OIDC and real-screen role execution is tracked under SURPASS-SEC |
| STUDIO-CORE-008 | Full visual JPQL designer: metadata-aware completion, joins, parameters, fetch plans, validation, execution console and safe query insertion | PARTIAL | Repository JPQL now shares an entity-aware backend/web/native model for roots, joins including outer joins, aliases, nested mapped paths, named/positional parameters, result shape, query properties and fetch-plan compatibility. Java/Kotlin `@Query` property segments provide completion, navigation, Find Usages, inspection, quick fix and rename. The analyzer is deliberately bounded: complete JPQL grammar/injection for subqueries, constructor projections, `TREAT`, map functions and correlated alias scoping, native `@Param` refactoring, execution console and multi-store runtime fixtures remain |
| STUDIO-CORE-009 | Framework-aware hot deploy for supported UI, messages, security, configuration and application changes with explicit restart boundaries | PARTIAL | Target-version runtime matrix proving every supported reload class |
| STUDIO-CORE-010 | BPMN modeler compatible with Jmix BPM/Flowable artifacts, forms, listeners, DMN, deployment and source round trip | PARTIAL | Deployed runtime operations and version-migration fixtures |
| STUDIO-CORE-011 | Live database reverse engineering, partial reverse engineering, entity/schema mapping, type mapping and safe regeneration | PARTIAL | The active-profile, project-driver JDBC backend browses bounded catalogs, schemas, tables and views without moving credentials or connection URLs into JCEF, including datasource-only stores with Liquibase disabled. Handwritten Java/Kotlin `@Table` metadata retains `name`, `schema`, and `catalog`. Digest snapshots cover columns, PKs, imported FKs, unique constraints, indexes and dependencies; source merge unlocks only for an exact entity/store/table/schema/catalog mapping. Database-first import supports ordered composite IDs, multi-column joins, strict pure join-table recognition, bounded recursive FK closure, exact existing-entity reuse, qualified many-to-many mappings, explicit stable view IDs, persistence-read-only computed columns, atomic Java/Kotlin/message generation and deterministic credential-free mapping profiles with drift review. A clean real matrix now certifies PostgreSQL 16.9, MySQL 8.4.6, MariaDB 11.4.8, SQL Server 2022 CU20 and Oracle 23.7 through the production service on IDEA 2025.3 and 2026.2, including project-property/library-driver resolution, schema browsing, constraint reconstruction, dependency closure, import planning and redaction. Complete vendor-specific type coverage, arbitrary handwritten partial-import shapes, richer override/regeneration UX and customer-schema certification remain; see `ENTITY-DESIGNER-PARITY.md` and `DATABASE-RUNTIME-CERTIFICATION.md` |
| STUDIO-CORE-012 | Framework-specific coding assistance: completion, references, injections, line markers, navigation, inspections, intentions, quick fixes and safe refactorings | PARTIAL | Native FlowUI XML IDs, Java/Kotlin controller annotations, entity classes, nested entity properties, shared/inline fetch plans, localized polyvariant message keys, view/screen IDs, nested menu IDs, Spring menu bean names/methods, resource/row policy targets and specific permissions provide completion, navigation, Find Usages, project-wide cross-file rename, bidirectional controller/descriptor gutters, ambiguity diagnostics and quick fixes. Java/Kotlin `@ViewComponent`, `@Subscribe`, `@Install` and `@Supply` contracts add native placement, signature, EventObject, target, duplicate, functional-interface and generic-injection validation. Exact XML injection types come from the opened project's Jmix/add-on Studio metadata rather than a bundled core-only table. Logical `@Subscribe(subject)` values provide compatible listener completion, navigation and rename, event/ambiguity validation and add-on `customSubscriptions`; the View Designer consumes the same Java issues and Kotlin read-only issue inventory. Premium-style `@UiComponentPolicy` references and inspections resolve the selected view, view actions, component actions and recursively nested fragments in Java/Kotlin; dotted paths support native completion, navigation, Find Usages, nearest-ID fixes and safe XML-policy refactoring, while the visual Security Workspace uses the same composed graph and contract findings. Jmix `rest-services.xml` adds Java/Kotlin Spring bean, method, overload, positional payload-parameter and JVM-type completion/navigation/Find Usages/safe rename plus fail-closed mapping diagnostics. `@RestService`, recursively composed Java/Kotlin Spring stereotypes, standard component-name `@AliasFor` aliases and Java/Kotlin `@Bean` factory products share the indexed model. Profile properties resolve, refactor and validate services/query descriptors, and Java/Kotlin event inspections enforce exact entity and transaction contracts. Ten independent persistent candidate indexes plus an annotation-name-keyed composed-stereotype usage index replace global PSI invalidation and broad file enumeration; an enforced source/descriptor architecture guard plus a dual-host 3,000-file, sixteen-module-shaped benchmark prove cross-artifact cache isolation, targeted relevant-edit invalidation and warm/typing p50-p95-p99 latency. Application-event publisher/listener links, other service consumers, the full properties/YAML/placeholder surface, language injections, more intentions and installed-IDE cold-index/memory/leak proof remain |
| STUDIO-CORE-013 | Context-aware Jmix code snippets and generators that use project conventions and never emit placeholder production behavior | PARTIAL | Discoverable editor actions plus target-toolchain compilation |
| STUDIO-CORE-014 | AI assistance with explicit consent, source/privacy boundaries, reviewable changes, offline-disabled behavior and no required proprietary runtime | MISSING | Threat model, privacy controls, evaluation suite and deterministic fallback |

## B. Project management and environment parity

| ID | Mandatory capability | Current status | Completion evidence required |
|---|---|---|---|
| STUDIO-PROJECT-001 | New-project wizard for certified Jmix versions, Java 17 through the latest certified Java, repositories, SDKs and application/add-on/composite templates | PARTIAL | A native IntelliJ New Project generator is registered and packaged in the shared, IDEA 2025.3 and IDEA 2026.2 descriptors. Its deterministic create-only engine validates coordinates/locales, accepts credential-free HTTPS repositories, rejects uncertified Jmix/Java combinations and unsafe paths, pins the Jmix dependency graph plus Gradle distribution checksums, ships its own integrity-checked wrapper, applies the selected SDK, links the build through IntelliJ's public Gradle settings API, and rolls back partial installation without touching pre-existing IDE files. The wizard offers Java/Kotlin plus headless/FlowUI choices. FlowUI applications include a responsive theme, main/login/welcome views, three-level menu, localized bundles, entity-backed production users, randomized or environment-controlled local credentials, menu/view/resource roles and versioned Liquibase. Application, source-first add-on and connected composite templates compile, test and—where runnable—start in all 40 combinations across Jmix 2.8.2 Java 17/21 and Jmix 3.0.0 Java 21/25. Runtime selection is literal: Java 25 templates build with JDK 25, emit Java/Kotlin 21-compatible bytecode and start on Java 25. Composite module ordering is explicit and certified. Native organization-template settings manage Ed25519 trust anchors, exact catalog versions, SHA-256-pinned HTTPS sources, explicit refresh/import and offline mode. The wizard dynamically lists compatible signed cached baselines and re-verifies immutable coordinates before generation; eight additional Java/Kotlin FlowUI cells pass real signed-catalog authoring, application, frontend build, Liquibase and startup across the four certified Jmix/JDK cells. A clean dual-host release gate passes 357 regression plus 3 smoke tests per host, compatible Plugin Verifier checks and fresh nested ZIP inspection. Installed-wizard keyboard, screen-reader, validation and recovery certification remains; see `PROJECT-TEMPLATE-CERTIFICATION.md` and `ORGANIZATION-TEMPLATE-CATALOGS.md` |
| STUDIO-PROJECT-002 | Jmix semantic project window with modules, data, UI, security, workflows, integrations, migrations and diagnostics | STRONG | Scale and accessibility proof remains under CERT-SCALE |
| STUDIO-PROJECT-003 | Project-properties editor for Jmix/add-on versions, repositories, Java/Kotlin, data stores and shared composite settings | PARTIAL | A bounded revision-bound inventory maps imported modules and external/composite content roots to Groovy/Kotlin builds, settings, Gradle properties, version catalogs and profile-specific application properties without repository-wide rescans or Gradle execution. The responsive Project Configuration workspace shows effective/conflicting Jmix and Java versions, add-ons, source navigation, active profiles, locales, server settings and main/additional/undeclared data stores. Existing indexed properties profiles now have exact-range editing for server, locale, active-profile, additional-store, JDBC, driver and Liquibase values. Secret changes accept environment-variable references only; JCEF receives a selected-key focused preview rather than the complete potentially secret-bearing file. Duplicate/continued/unsupported source, unindexed targets, stale revisions and forged digests fail closed; atomic apply plus exact visual undo/redo preserve comments, separators, ordering, Unicode and CR/LF/CRLF. Native Tools access, both host descriptors and nested ZIP gates enforce discovery and mutation packaging. Thirteen focused tests pass per host; the clean 367-regression plus 3-smoke gate, both Plugin Verifier lanes, production frontend and package-contract checks pass; browser evidence proves zero overflow and 40-pixel controls at 1280/640/440/320 pixels. Complete previewed Gradle/settings/version/dependency mutations, locale formats, profile/environment comparison, data-store lifecycle/configuration classes/connection tests and installed accessibility certification remain; see `PROJECT-PROPERTIES-PARITY.md` |
| STUDIO-PROJECT-004 | Add-on marketplace/catalog discovery, compatibility resolution, install, configure, remove and upgrade, including private organization catalogs | MISSING | Signed metadata, dependency-conflict handling and rollback tests |
| STUDIO-PROJECT-005 | Plugin settings, per-project settings, safe settings migration, export/import and enterprise policy locks | PARTIAL | Versioned schema, migration and corrupted-settings recovery tests |
| STUDIO-PROJECT-006 | Composite projects and included builds: create/add/remove subprojects, dependency graph editing, cycle prevention and common upgrades | PARTIAL | Atomic Gradle plus `@JmixModule` changes across real composite fixtures |
| STUDIO-PROJECT-007 | Profile-specific properties and external `.env` editing with secret detection, redaction and environment comparison | PARTIAL | Indexed `application[-profile].properties` discovery and exact source navigation now join server/locale/active-profile/data-store editing through selected-key secret-safe preview, stale-bound atomic apply and undo/redo. Literal password/default secrets and embedded URL credentials are denied at the backend boundary. Profile creation/removal, cross-environment comparison, external `.env` editing, runtime activation validation and installed-IDE recovery remain |
| STUDIO-PROJECT-008 | Welcome/onboarding experience for create, open, import, diagnose and recover existing Jmix projects | MISSING | First-run accessibility and failure-recovery journeys |
| STUDIO-PROJECT-009 | Complete localization tooling: message discovery, locale matrix, missing/unused keys, safe rename, fallback preview and translation import/export | PARTIAL | Native `msg://`, `msg:///`, `Messages` and `MessageBundle` completion, navigation, Find Usages, rename and unresolved-key diagnostics are verified across XML, Java and Kotlin on both IDE hosts. Multi-locale matches are valid polyvariant resolutions rather than false unresolved errors; declaration-side rename updates every writable sibling locale and all three usage languages. Complete the visual cross-module locale matrix, fallback preview, missing/unused-key analysis and translation import/export |
| STUDIO-PROJECT-010 | Jmix and add-on upgrade assistant with release notes, compatibility adapters, migrations, build verification and exact rollback | MISSING | Real sequential upgrades across every certified version family |

## C. Advanced Studio parity

| ID | Mandatory capability | Current status | Completion evidence required |
|---|---|---|---|
| STUDIO-ADV-001 | OpenAPI-first client generation with authentication, configuration, model customization, regeneration safety and contract testing | MISSING | Multiple OpenAPI-version fixtures and generated-client compilation |
| STUDIO-ADV-002 | Data-repository creation, method/query design, injection, view delegate integration and refactoring | SUBSTANTIAL | The shared Java/Kotlin generator and responsive editor cover derived/JPQL methods, typed parameters, bindings, projections, fetch plans, hints, documentation and constraint policy. Existing supported method metadata is edited only at exact annotation ranges; callable source, comments, `@Override`, modifiers, custom annotations, nullability and bindings are preserved, while unknown/comment-bearing constructs fail closed. Official context-preserving collection load delegates and bounded detail save delegates inject repositories into revision-checked Java and Kotlin controllers. Transactional aggregate services preserve the whole `SaveContext`, use constrained `DataManager`, choose the exact entity-store transaction manager, register platform update delegates when available, and wire Java/Kotlin DataContext handlers as one previewed atomic change; stale/forged evidence, conflicting existing logic, collisions and cross-store pseudo-transactions fail closed. The native IntelliJ Generate action discovers module-accessible repositories through inheritor indexes, resolves custom generic bases, excludes repository fragments and adds idempotent Java/Kotlin PSI injection in one rollback-protected undoable command. Disabled or unproven inherited constraint policy requires a native warning decision. Effective `@ApplyConstraints` is evaluated for the exact generated `findAll`/`save` invocation with method precedence and resolvable hierarchy analysis; unproven hierarchies are denied and no bridge override exists. Existing Java/Kotlin methods expose exact-revision source navigation, Rename, Change Signature and Safe Delete, with qualified-owner/signature/range proof before IntelliJ owns preview and undo. Java/Kotlin query properties and named parameters provide native completion/navigation/inspection/quick-fix/Find-Usages/rename. Generated Java/Kotlin repository and controller contracts compile against all four certified Jmix/JDK cells. Complete JPQL semantics and installed-IDE interaction certification remain |
| STUDIO-ADV-003 | Custom project-template creation, validation, versioning, organization catalog and offline use | PARTIAL | The typed deterministic publisher creates ordered declarative schema-v2 bundles with exact text and binary payloads, computes per-file digests, signs exact manifests, immediately self-verifies through the production boundary and writes create-only output; legacy schema-v1 text catalogs remain compatible. Strict schema/duplicate/path/size/inventory/UTF-8 checks, signer/catalog allowlists, expiry, semantic anti-rollback floors, exact bundle SHA-256 pins, immutable cache coordinates and re-verification on every offline open fail closed. Native Tools settings provide trust/source/version configuration, explicit HTTPS refresh, signed local import and offline mode; the project wizard dynamically lists only compatible cached templates and rejects stale selection. The native `Create Signed Bundle…` workflow scans an exact certified base against a customized project, ignores generated caches, rejects links and non-portable or case-colliding paths, shows a sortable side-by-side text/binary/executable review, then re-scans immediately before signing so stale source, content or permissions fail closed. Local PEM/DER PKCS8 keys are transient and zeroed; the packaged signing-provider extension supports PKCS11, HSM, secure-enclave or remote signers without exporting private keys. Eight real Java/Kotlin FlowUI cells pass authoring, signature verification, exact binary installation, Vaadin build, Liquibase and Jmix startup across Jmix 2.8.2/3.0.0 and Java 17/21/25. Both host descriptors and installable ZIP gates enforce the native authoring, overlay and signer components. Installed-IDE keyboard, screen-reader, cancellation and recovery certification remains; see `ORGANIZATION-TEMPLATE-CATALOGS.md` |
| STUDIO-ADV-004 | Docker and cloud deployment designer with environment configuration, secrets, health checks, migrations, rollback and portable output | MISSING | Local container plus supported-provider deployment rehearsals |
| STUDIO-ADV-005 | Jmix REST/OpenAPI service authoring, saved invocation, authorization, transactions, contract evolution and client generation | PARTIAL | Provider/consumer contract and compatibility suites |

## D. Required capabilities that surpass Studio

| ID | Mandatory differentiator | Current status | Completion evidence required |
|---|---|---|---|
| SURPASS-GRAPH-001 | Whole-application, multi-build graph connecting entities, views, services, methods, REST, security, workflows, integrations, jobs, reports, migrations, configuration and tests | STRONG | Deeper runtime/dataflow evidence is tracked in CERT-SCALE and CERT-RUNTIME |
| SURPASS-GRAPH-002 | Transitive impact analysis, semantic change preview and explanation of editable/read-only/blocked ownership | STRONG | Representative customer-repository certification |
| SURPASS-LOGIC-001 | Typed visual server logic, reusable subflows, collections, exceptions, transactions, authorization and version-aware generated source | STRONG | Queue/event primitives and target-dependency semantic compilation remain active gaps |
| SURPASS-LOGIC-002 | Rules, formulas, decision tables/trees, reusable rule sets, lifecycle governance and tenant-aware rollout | PARTIAL | Trees/rule sets and deployed-version migration |
| SURPASS-INT-001 | Visual connector builder for HTTP/webhooks, Kafka, RabbitMQ, SFTP, email, file/object storage, SMS, payments and identity | ACTIVE | All listed adapter families now use the source-safe visual pipeline, including identity connectors bound to an indexed OAuth2 client manager; organization catalog, exact version adapters and real provider fixtures remain |
| SURPASS-INT-002 | Reliability design: bounded timeouts, retry/backoff, circuit breaker, bulkhead, rate limits, idempotency, transactions, outbox, ordering, DLQ and replay | ACTIVE | Typed policies and fail-closed conflict checks now generate reviewed adapter/config pairs; persisted outbox, Rabbit retry infrastructure, replay, provider observability and fault injection remain |
| SURPASS-INT-003 | Organization connector catalog with signed templates, policy enforcement, versioning, secrets and approval workflow | MISSING | Signed catalog and multi-team governance tests |
| SURPASS-RUNTIME-001 | Runtime workflow/logic debugger with breakpoints, variables, watches, transaction/SQL traces, async replay and audit timeline | MISSING | Production-safe local/remote runtime fixtures |
| SURPASS-SEC-001 | Effective security simulation for menu, view, entity, attribute, specific, row-level, REST and workflow access by role/user/organization/context | PARTIAL | OIDC claims and real-screen/server execution evidence |
| SURPASS-QUALITY-001 | Financial and production diagnostics: unsafe money, duplicated calculations, missing server validation/transactions/idempotency, workflow bypass, broken JPQL/fetch plans, logging and schema risks | PARTIAL | Cross-method duplicate-calculation and full job/integration analysis |
| SURPASS-SIM-001 | Visual integration failure simulation for timeouts, partial failure, duplicates, redelivery, reordering, DLQ, compensation and provider outage | MISSING | Deterministic plus real-container fault suites |
| SURPASS-TEST-001 | Recorded UI/API/workflow/migration scenarios with isolated seeds, eventual assertions, security contexts and generated readable tests | PARTIAL | Real recording/execution and async/runtime steps |
| SURPASS-GOV-001 | Semantic Git diff, visual merge, ownership, approval policy, audit export and CI quality gates | MISSING | Concurrent-team and protected-branch fixtures |
| SURPASS-FIELD-001 | Offline/low-bandwidth field tooling, sync/conflict design, data packages, geospatial/case/beneficiary support and device diagnostics | MISSING | Disconnected and conflict-heavy reference scenarios |
| SURPASS-OPEN-001 | Generated applications remain normal, readable Jmix source with no proprietary workbench runtime dependency | STRONG | Enforced continuously by every generator and release test |

## E. Compatibility, proof and productization gates

| ID | Mandatory proof gate | Current status | Completion evidence required |
|---|---|---|---|
| CERT-WRITE-001 | Every write path uses compatibility selection, immutable visible diff, stale rejection, atomic apply, rollback and undo; no hidden legacy generator path | PARTIAL | The shared visual mutation boundary now performs a second revision preflight under IntelliJ's write lock, captures every original document/VFS value and missing parent directory, verifies every approved result, propagates cancellation, and restores plus verifies the complete pre-mutation workspace on one-shot write or undo/redo failure. Dual-host injected-failure cases prove partial multi-file apply, cancellation, concurrent edit, failed undo and complete undo/redo leave either the exact approved result or the exact prior file/directory topology. Native Java/Kotlin repository injection performs the same locked-document recheck and exact PSI/document restoration. A release-blocking source inventory rejects any new `WriteCommandAction`, direct VFS save/create/delete or document replacement outside these certified boundaries and proves the fault probe is internal and absent from JCEF. Persistent operating-system failures that also prevent restoration, installed-IDE crash/power-loss recovery, compatibility selection for every legacy command and IDE-owned refactor certification remain; therefore this gate is not yet STRONG |
| CERT-JAVA-001 | Java 17 through the latest declared Java version | STRONG | Production-generated Java/Kotlin entities, advanced repositories, FlowUI controllers and aggregate update services compile under strict verification against Jmix 2.8.2 on JDK 17/21 and Jmix 3.0.0 on JDK 21/25; class majors 61/65/69, exact compiler runtimes, source digest and dependency-graph digests are release evidence. Application startup/runtime and database cells remain under CERT-RUNTIME/DB rather than weakening this compile gate |
| CERT-KOTLIN-001 | Kotlin discovery and editing wherever Kotlin support is declared | PARTIAL | Entity generation, source-set targeting, repository generation, Kotlin schema-metadata reconstruction, revision-bound additive handwritten-entity editing, managed mapping evolution, native stable-column property rename, project-wide scalar Type Migration through Kotlin light fields, live-verified annotation-only expansion cutover, native Safe Delete preparation and atomic explicit scalar plus owning-to-one physical-column rename now pass on both host lanes. Generated Kotlin entity/repository/aggregate-service contracts also compile in all four exact Jmix/JDK cells. Relationship-shape, final contraction/retirement and full target-project semantic coverage remain |
| CERT-JMIX-001 | Explicit Jmix 2.x/3.x version adapters; only tested cells are advertised | PARTIAL | The published matrix pins and compiles exact 2.8.2 and 3.0.0 framework graphs with line-aware aggregate delegate generation. Real applications for both lines now start and exercise Jmix enhancement, metadata and `DataManager` on five certified databases under the four JDK cells in `DATABASE-RUNTIME-CERTIFICATION.md`. Complete operation-by-operation adapters and representative customer-project fixtures remain |
| CERT-IDE-001 | Installed-plugin tests for every declared IntelliJ IDEA version | PARTIAL | The native FlowUI provider, document lifecycle, host descriptor, full test suite, host smoke test, packaged ZIP and Plugin Verifier gates pass on IntelliJ 2025.3 and 2026.2. Clean installed-IDE Design/XML interaction, startup, UI automation, memory and leak tests remain |
| CERT-DB-001 | Certified PostgreSQL, MySQL/MariaDB, MSSQL and Oracle behavior where advertised | STRONG | Clean Docker fixtures certify PostgreSQL 16.9, MySQL 8.4.6, MariaDB 11.4.8, SQL Server 2022 CU20 and Oracle 23.7 across Jmix 2.8.2/3.0.0 and Java 17/21/25. Sixty runtime documents prove startup, enhancement, `DataManager`, baseline, forward/rollback/reapply and unlocked Liquibase state; ten dual-host documents prove production reverse engineering. See `DATABASE-RUNTIME-CERTIFICATION.md`; arbitrary vendor/customer schemas remain bounded by the advertised scope |
| CERT-SCALE-001 | Deterministic onboarding and editing of 16–100-module, 3,000+ file, multi-store, add-on-heavy projects | PARTIAL | Synthetic 16-module topology and 3,000-unrelated-file native-index fixtures pass on both hosts; payroll fixture plus anonymized representative enterprise suites remain |
| CERT-PERF-001 | Published indexing, UI latency, generation, memory and leak budgets | PARTIAL | Dual-host 3,000-file/sixteen-module-shaped regression proves 100 warm inventory reads in 6/6 ms total, unrelated-edit access in 2/2 ms and twenty three-file typing cycles in 76/88 ms total without cross-artifact eviction; p50/p95/p99 budgets are enforced. Add installed-IDE cold indexing, completion/navigation, generation, memory, cancellation and leak baselines |
| CERT-RUNTIME-001 | Parse, compile, reopen and representative runtime validation after each generated change class | PARTIAL | Exact generated entity/repository/controller/service contracts compile in four Jmix/JDK cells, and a real Jmix application starts plus exercises persistence and migrations across five databases. Runtime/reopen coverage for every remaining generated change class and installed target applications remains |
| CERT-SEC-001 | Plugin threat model, secure defaults, dependency scanning, secret protection and security regression tests | PARTIAL | Reviewed threat model and release-blocking AppSec gates |
| CERT-ACCESS-001 | Keyboard operation, screen-reader semantics, responsive layouts and accessibility checks for every designer | PARTIAL | The isolated native FlowUI editor has measured zero shell overflow at 1280/640/440/320 pixels, permanent no-tab regions, usable stacked-region heights and accessible region labels; insertion and undo are keyboard-addressable buttons. Complete automated and manual WCAG-oriented audits across every workspace remain |
| CERT-RELEASE-001 | Signed Marketplace artifact, reproducible build, SBOM, provenance, third-party notices, update channels and rollback | MISSING | Independently verifiable release pipeline |
| CERT-NODE-001 | Self-sustained frontend build without a preinstalled Node.js, with integrity verification and offline strategy | STRONG | Keep dependency provenance and offline cache verification current |
| CERT-DOCS-001 | User, administrator, migration, compatibility, troubleshooting and extension documentation with maintained @examples | MISSING | Versioned documentation release and support runbook |

## Execution rule

Implementation order may change to reduce risk, but requirements may not be
removed or silently weakened. Any status change MUST update
`ENTERPRISE-PARITY-AUDIT.md` with concrete evidence and remaining gaps.

The current active tranche is `SURPASS-INT-001` and `SURPASS-INT-002`. The
first native-editor tranche of `STUDIO-CORE-012` is also implemented and
verified on both supported IntelliJ hosts. The next release audit MUST
reconcile all newly completed work against `STUDIO-*`, `SURPASS-*`, and
`CERT-*` identifiers.
